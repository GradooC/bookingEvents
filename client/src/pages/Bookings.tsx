import React from 'react';

interface BookingsProps {}

const Bookings: React.FC<BookingsProps> = props => {
  // const {  } = props;
  return (
    <h1>The Bookings Page</h1>
  );
};

export default Bookings;